The V2 package is same as the SearchTree and CPS style coding of the FinallyTagless implementation.

In this package the common code is factored out and class is used to capture the interpreter constructs